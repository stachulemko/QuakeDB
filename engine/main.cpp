
#include "column.h"
#include "tlv.h"
#include "dataBase.h"
int main() {
	/*
	Database db;
	db.addTable("testTable");
	db.addTable("negro");
	db.addColumn("negro", "sd", int32_tId, true);
	db.addColumn("testTable", "id", int32_tId, false);
	db.addColumn("testTable", "name", stringId, true);
	db.addColumn("testTable", "age", int32_tId, false);
	db.addRecord("testTable", { 1, "Alice", 30 });
	db.addRecord("testTable", { 2, "Bob", 25 });
	db.addRecord("testTable", { 3, "Charlie", 35 });
	db.commit();
	*/

	/*
	Database db;
	db.loadDataBase();
	db.select("testTable", { "id", "name" });
	db.where("age", ">", 25);
	db.showSqQuery();
	std::cout << "======================" << std::endl;
	db.showWal();
	*/
	
}
